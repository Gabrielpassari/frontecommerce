import Image from 'next/image';
import Link from 'next/link';

export default function Hero() {
  return (
    // 1. Container Principal (fundo claro, padding generoso)
    <section className="w-full bg-gray-50 py-16 px-6 md:px-12 lg:py-24">
      
      {/* 2. Wrapper Interno (limita a largura e centraliza) 
         Usa Flexbox: Coluna no mobile, Linha no desktop (md:flex-row) */}
      <div className="max-w-7xl mx-auto flex flex-col md:flex-row items-center gap-12">
        
        {/* 3. Coluna da Esquerda: Textos (Ocupa metade da largura no desktop) */}
        <div className="flex-1 space-y-6 text-center md:text-left">
          
          {/* Título Principal (H1): Grande, Negrito, Leading apertado */}
          <h1 className="text-5xl md:text-6xl font-extrabold text-gray-900 leading-tight">
            Nova Coleção <span className="text-blue-600">Verão 2024</span>
          </h1>
          
          {/* Subtítulo (P): Texto menor, cor suave para contraste */}
          <p className="text-xl text-gray-600 max-w-2xl mx-auto md:mx-0">
            Descubra as últimas tendências e encontre o estilo perfeito para você com preços imbatíveis.
          </p>
          
          {/* Botão de Chamada para Ação (CTA) */}
          <div className="pt-4">
            <Link href="/produtos" 
              className="inline-block bg-blue-600 text-white px-8 py-3 rounded-full font-semibold text-lg hover:bg-blue-700 transition duration-300 shadow-md">
              Comprar Agora
            </Link>
          </div>
        </div>

        {/* 4. Coluna da Direita: Imagem (Ocupa a outra metade no desktop) */}
        <div className="flex-1 flex justify-center">
          {/* Wrapper da imagem com position relative para o fill dar certo */}
          <div className="relative w-[300px] h-[300px] md:w-[450px] md:h-[450px]">
            <Image 
              src="/heroproduct.png" // Nome da imagem na pasta public (certifique-se que ela existe)
              alt="Modelo em destaque na coleção"
              fill // Preenche o espaço da div pai
              className="object-cover rounded-2xl shadow-xl" // Estilização visual da imagem
              priority // Diz ao Next para carregar essa imagem primeiro (bom para SEO/LCP)
            />
          </div>
        </div>

      </div>
    </section>
  );
}